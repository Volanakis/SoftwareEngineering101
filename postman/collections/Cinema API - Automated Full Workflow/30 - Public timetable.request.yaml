$kind: http-request
url: http://127.0.0.1:5000/programs/{{programId}}/screenings?view=timetable
method: GET
queryParams:
  view: timetable
scripts:
  - type: afterResponse
    code: |-
      pm.test('Public timetable loads', () => pm.response.to.have.status(200));
      pm.expect(pm.response.json().results).to.be.an('array').that.is.not.empty;
    language: text/javascript
order: 30000
