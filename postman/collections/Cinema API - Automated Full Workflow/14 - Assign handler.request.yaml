$kind: http-request
url: http://127.0.0.1:5000/programs/{{programId}}/screenings/{{screeningId}}/handler
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "userId": "{{staffId}}"
    }
scripts:
  - type: afterResponse
    code: pm.test('Handler assigned', () => pm.response.to.have.status(200));
    language: text/javascript
order: 14000
